/**
 * pi-briefly
 *
 * Initial implementation placeholder. The extension will briefify completed
 * tool calls while leaving active tool output visible.
 */
import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function briefly(_pi: ExtensionAPI) {
  // Implementation follows after the renderer/invalidation strategy is settled.
}
