{
  "name": "pi-briefly",
  "version": "0.1.0",
  "description": "Briefify completed Pi tool calls while preserving live output during execution.",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --experimental-strip-types --test"
  },
  "keywords": ["pi-package", "pi", "pi-extension", "tui", "tools"],
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  },
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "license": "MIT"
}
